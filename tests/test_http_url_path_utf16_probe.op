import http

function int main() {
    let parsed = http_parse_url("https://example.com")
    if parsed == 0 {
        return 1
    }
    let wide = http_url_path_utf16(parsed)
    if wide == 0 {
        return 2
    }
    return 3
}
