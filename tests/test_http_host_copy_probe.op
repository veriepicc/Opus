import http

function int main() {
    let parsed = http_parse_url("https://example.com")
    if parsed == 0 {
        return 1
    }
    let host_text = parsed.host
    if host_text == 0 {
        return 2
    }
    return 3
}
