struct Pair {
    int left
    int right
}

function int sum(*Pair p) {
    return p.left + p.right
}

function int main() {
    var p = Pair { left: 10, right: 20 }
    let v = sum(p)
    free(p)
    if v != 30 {
        return 1
    }
    return 0
}
