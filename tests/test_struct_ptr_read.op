struct Pair {
    int left
    int right
}

function int read_left(*Pair p) {
    return p.left
}

function int main() {
    var p = Pair { left: 10, right: 20 }
    let v = read_left(p)
    free(p)
    if v != 10 {
        return 1
    }
    return 0
}
