import http

function int main() {
    let parsed = http_parse_url("https://example.com")
    if parsed == 0 {
        return 1
    }
    let wide = http_utf16z(parsed.host)
    if wide == 0 {
        return 2
    }
    return 3
}
