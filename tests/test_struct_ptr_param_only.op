struct Pair {
    int left
    int right
}

function int keep(*Pair p) {
    return 7
}

function int main() {
    var p = Pair { left: 10, right: 20 }
    let v = keep(p)
    free(p)
    if v != 7 {
        return 1
    }
    return 0
}
